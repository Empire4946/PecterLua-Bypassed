-- Credits to the Owner

local no
no = hookfunction(syn.request, function(...)
local Args = {...}
local Self = Args[1]
if string.find(Self['Url'], 'webhooks') then
return print('lol no\n')
end
return no(...)
end)
